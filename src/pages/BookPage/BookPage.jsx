import './BookPage.css';
import { BookPageContainer } from '../../containers/BookPageContainer';

export const BookPage = ()=>{
    return (
        <div>
            <BookPageContainer/>
        </div>
    );
}